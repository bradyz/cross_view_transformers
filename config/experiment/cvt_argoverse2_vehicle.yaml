# @package _global_
defaults:
  - override /model: cvt
  - override /data: argoverse2_vehicle

  - override /loss: center_loss
  - override /metrics: visibility_metrics

model:
  outputs:
    bev: [0, 1]
    center: [1, 2]
